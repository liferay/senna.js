'use strict';

module.exports = {
  setUp: function(done) {
    done();
  },

  tearDown: function(done) {
    done();
  },

  testShouldSomething: function(test) {
    test.ok(true, 'Should something.');
    test.done();
  }
};
